language: python
python:
  - "3.6"
  - "3.7"

addons:
  apt:
    packages:
    - make
    - g++
    - libz-dev
install:
  - pip install -r build_requirements.txt
script:
  - make test
addons:
  apt:
    packages:
    - make
    - libz-dev
    - 
